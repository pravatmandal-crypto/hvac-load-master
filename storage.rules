rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Project drawings — only authenticated users with design-team or admin roles
    match /projects/{projectId}/drawings/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
